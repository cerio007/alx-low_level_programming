#include "main.h"

/**
 * swap_int - swap the values of two integers
 * @a: the first integer
 * @b: the second integer
 * Return: Always 0
 */
void swao_int(int *a, int *b)
{
	int c = *a;
	*a = *b;
	*b = c;
}
