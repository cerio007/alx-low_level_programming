#include <stdio.h>

/**
 * main - main block
 * Description: Print all numbers of base 10 starting from 0.
 * Return: 0
 */
int main(void)
{
	int z;
	for (z = 0; z < 10; z++)

		printf("%i", z);
	return (0);
}
